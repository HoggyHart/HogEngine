
UI entities must all be added to a PANEL

the PANEL will have a container component with a LAYOUT object that should dictate the size and position of UI entities inside of it such as lists and buttons